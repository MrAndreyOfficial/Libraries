This library contains extension methods for `strings`.

This library is located in the `String_Library` namespace.

Methods:
1. Append(`string` value) returns `string`
2. Remove(`int` startIndex, `int` endIndex) returns `string`
3. GetFirstChar returns `char`
4. GetLastChar returns `char`
